# B20.6 — Security Investigation

## Status

**PASS — Security Investigation Context & B19 RAG Integration**

B20.6 establishes the TAP B20 security investigation context layer.

The component prepares structured investigation context from authorized security evidence. It does not perform AI reasoning and does not authorize or execute security actions.

## 1. Purpose

B20.6 provides the investigation context layer between security evidence and the B20.5 AI Security Operator.

B20.6 prepares structured investigation context. It does not perform AI reasoning, authorize remediation, or execute infrastructure changes.

## 2. Responsibilities

- identify investigation, environment, asset, and question
- validate authorized SecurityEvidence objects
- validate UTC timestamps
- record missing evidence
- provide deterministic evidence summaries
- remain provider-neutral
- accept and validate the existing B19 `AIContextRequest`
- consume RAG context without owning RAG retrieval
- request relevant B19 security knowledge through the `SecurityRAGAdapter`
- pass retrieved B19 context into the existing provider-neutral `AIContextRequest` contract

## 3. Architecture

B20.4 Monitoring → B20.3 Security Evidence → B19 RAG → B20.6 Investigation → B20.5 AI Security Operator → TAP Policy/Authorization → Governed Action → Verification → Audit

## 4. Security Boundary

B20.6 does not grant execution authority. AI recommendations remain subject to TAP policy, authorization, execution controls, verification, and recording.

## 5. B19 RAG Integration

B20.6 now performs live integration with the existing B19 RAG architecture through `SecurityRAGAdapter`.

The integration flow is:

B20.6 Security Investigation
→ `RetrievalQuery`
→ B19 `RetrievalEngine`
→ B19 `ContextAssembler`
→ B19 `RAGContext`
→ B19 `AIContextRequest`
→ B20.5 AI Security Operator

B19 remains responsible for knowledge retrieval, deterministic ranking, context assembly, provenance, and the provider-neutral `AIContextRequest` contract.

B20.6 remains responsible for the security investigation workflow and the mapping of a security investigation into a B19 retrieval request. It does not create a second RAG system.

The current retrieval implementation is deterministic metadata/title/token matching with investigation asset and knowledge-type filters. It does not yet use embeddings or vector search.

The current `AIContextRequest` contains retrieved knowledge metadata and provenance. Authoritative document-content resolution through the existing B19 `KnowledgeContentResolver` is a future increment if required for full AI prompt context.

B20.5 local Qwen3:4b full investigation execution remains deferred because of the documented local AI runtime capacity limitation.

## 6. Validation

B20.6 investigation/RAG adapter tests: 10 passed.
Full B20 regression: 44 passed.
B20 source compilation: PASS.

B19 → B20.6 live retrieval integration: PASS.
B19 → B20.6 `AIContextRequest` integration: PASS.
Investigation ID consistency validation: PASS.
Ranking and provenance preservation: PASS.
Invalid retrieval limit validation: PASS.

## 7. Result

B20.6 Security Investigation — PASS.
